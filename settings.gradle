rootProject.name = 'db-plaground'

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files('libs.versions.toml'))
        }
    }
}

include 'kafka'
include 'flink'
include 'dynamo'
